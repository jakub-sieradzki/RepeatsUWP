<Page
    x:Class="Repeats.Pages.EditItems"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Repeats.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar Background="{x:Null}">
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton x:Uid="AddButton" Name="ADD" Icon="Add" Label="" Click="Add_Click"/>
            <AppBarButton x:Uid="DeleteButton" Name="DELETE" Icon="Delete" Label="" Click="Delete_Click"/>
            <AppBarButton x:Uid="SaveButton" Name="SAVE" Icon="Save" Label="" Click="Save_Click"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView Name="LISTItems" Margin="25,30,20,65" IsItemClickEnabled="True" ItemClick="Item_Click"/>
        <ProgressRing Name="Ring" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="100"/>
    </Grid>
</Page>
